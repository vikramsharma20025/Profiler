import React from 'react';
// import { auth } from '@/auth';
import Image from 'next/image';

const page = async () => {
    // const session = await auth();
    return (
        <div>
            
        </div>
    )
}   

export default page;